@using SourceCode.Models
<!--header-->
<div class="header">
    <div class="top-header navbar navbar-default">
        <!--header-one-->
        <div class="container">
            <div class="nav navbar-nav wow fadeInLeft animated" data-wow-delay=".5s">
                @if (Session["User"] != null && Session["User"] != "")
                {
                    <p>
                        <a href="@Url.Action("SignOut","Home")">Log out</a>
                    </p>
                }
                else
                {
                    <p>Welcome to the Realtors Portal <a href="/dang-ky">Register</a> or <a href="/dang-nhap">Log in</a></p>
                }
            </div>
            <div class="nav navbar-nav navbar-right social-icons wow fadeInRight animated" data-wow-delay=".5s">
                <ul>
                    <li><a href="#"> </a></li>
                    <li><a href="#" class="pin"> </a></li>
                    <li><a href="#" class="in"> </a></li>
                    <li><a href="#" class="be"> </a></li>
                    <li><a href="#" class="you"> </a></li>
                    <li><a href="#" class="vimeo"> </a></li>
                </ul>
            </div>
            <div class="clearfix"> </div>
        </div>
    </div>
    <div class="header-two navbar navbar-default">
        <!--header-two-->
        <div class="container">
            <div class="nav navbar-nav header-two-left">
                <ul>
                    @if (Session["User"] != null && Session["User"] != "")
                    {
                        User user = (User)Session["User"];
                        <input type="text" id="UserID" value="@user.ID" hidden />
                        <li><i class="glyphicon glyphicon-user" aria-hidden="true"></i><a href="@Url.Action("Index","User")">@user.FullName</a></li>
                        <br />
                        <li><i class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></i><a href="@Url.Action("CheckoutOrder","User")">My choice</a></li>
                        <br />
                        <li><i class="glyphicon glyphicon-gift" aria-hidden="true"></i><a href="@Url.Action("Index","Voucher")">Voucher</a></li>
                        <br />
                        <li><i class="glyphicon glyphicon-dashboard" aria-hidden="true"></i><a href="@Url.Action("ProductViewed","Product")">History</a></li>
                    }
                </ul>
            </div>
            <div class="nav navbar-nav logo wow zoomIn animated" data-wow-delay=".7s">
                <h1><a href="/trang-chu">REALTORS<b>PORTAL</b><span class="tag">Choose a beautiful house for you</span> </a></h1>
            </div>
            <div class="nav navbar-nav navbar-right header-two-right">
                <div class="header-right cart" id="DivCart">
                    @Html.Action("CartPartial", "Cart")
                </div>
                <div class="clearfix"> </div>
            </div>
            <div class="clearfix"> </div>
        </div>
    </div>
    @Html.Action("MenuPartial", "Home")
</div>
<!--//header-->